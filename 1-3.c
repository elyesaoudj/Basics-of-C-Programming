#include <stdio.h>
#include <stdlib.h>
int main(){
    int length,breadth;
    printf("enter length:");
    scanf("%d",&length);
    printf("enter breadth:");
    scanf("%d",&breadth);
    printf("the area of the rectangle is :%d ",length*breadth);
    return 0;
}
